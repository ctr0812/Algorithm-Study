package test.nc;

import java.util.Arrays;

public class N3 {
    public int solution(int[] paper, int n) {

        int[] temp = paper;
        Arrays.sort(temp);

        for(int i = 0; i < n; i++){

            // 인덱스 차이 홀수 && 두 값이 0 보다 클 경우 합한다
            if(temp[0] > 0 && temp[1] > 0){

            }
        }

        int answer = -2;
        return answer;
    }
}